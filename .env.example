PORT=3000
NODE_ENV=dev

# PostgreSQL remoto (schema agua)
DATABASE_URL=postgresql://postgres:CHANGE_ME@104.131.125.131:5432/alarms?schema=agua

JWT_ACCESS_SECRET=change_me_access_min_16_chars
JWT_ACCESS_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# Usado apenas pelo seed (primeiro admin)
SEED_ADMIN_EMAIL=admin@example.com
SEED_ADMIN_PASSWORD=ChangeMe123!

# Docker entrypoint: 1 para pular migrate deploy
SKIP_DB_MIGRATE=0
